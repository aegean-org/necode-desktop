---
name: pdf
description: Read, create, merge, and split PDF files with verified output.
---

Use `pdf_read` to inspect an existing PDF, `pdf_create` for a new verified PDF, `pdf_merge` to combine files in the requested order, and `pdf_split` for one-based page ranges. Use explicit output paths only when the user requests them.
