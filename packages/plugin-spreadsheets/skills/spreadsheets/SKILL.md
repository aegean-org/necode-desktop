---
name: spreadsheets
description: Create, read, and update verified XLSX workbooks with optional chart images.
---

Use `spreadsheet_read` for workbook metadata or an explicit A1 range. Use `spreadsheet_create` for new workbooks and `spreadsheet_update` for explicit cell or chart changes to a new verified file. Bar, line, and pie charts are embedded PNG images rather than native Excel chart objects.
