---
name: documents
description: Create, read, and revise DOCX files with verified output.
---

Use `document_read` before revising an existing file. Use `document_create` for new files. `document_revise` creates a new verified version from complete replacement content and does not promise lossless preservation of arbitrary third-party formatting.
